# base/40-secret.yaml — PLACEHOLDER only (inert under the smoke profile; the
# envFrom secretRef still needs to resolve). W6 D3 replaces this with an
# ExternalSecret backed by IRSA. namespace is set by the overlay.
apiVersion: v1
kind: Secret
metadata:
  name: expense-api-secrets
  labels:
    app.kubernetes.io/name: expense-api
    app.kubernetes.io/part-of: expense
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: placeholder-replaced-by-eso-in-w6d3
